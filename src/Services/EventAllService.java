package Services;

import Results.EventAllResult;

/**
 * A class for getting all events for all family members of the current user
 */
public class EventAllService
{
    /**
     * Creates an event all service object
     */
    public EventAllService()
    {

    }

    /**
     * Returns all events for all family members of the current user with this authentication token
     * @param token
     * @return
     */
    public EventAllResult event(String token)
    {
        return null;
    }
}
