package Services;

import Requests.RegisterRequest;
import Results.RegisterResult;

/**
 * A class handling all requests regarding user registration in the database
 */
public class Register
{
    /**
     * Default constructor to create an empty Register service object
     */
    public Register()
    {

    }

    /**
     * Register a new user in the database an creates generations and events
     * @param reguest
     * @return
     */
    public RegisterResult register(RegisterRequest reguest)
    {
        return null;
    }
}
